{
  "name": "docker-node",
  "version": "0.0.0",
  "description": "Official Docker Image for Node.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true
}
